{
  "author": "hoppingmode",
  "dependencies": {
    "sharp": "^0.33.0"
  },
  "description": "A utility toolbox for everyday desktop activities.",
  "main": "./dist/main/main.js",
  "name": "desktop-toolbox",
  "scripts": {
    "link-modules": "cmd /c mklink /J node_modules ..\\..\\node_modules",
    "postinstall": "npm run rebuild && npm run link-modules",
    "rebuild": "electron-rebuild -v 36.2.1 --force --types prod,dev,optional --module-dir ."
  },
  "version": "0.0.1"
}
